name: Build Oneplus 8 (OxygenOS 13.1 A13) (ReSukiSU+SUSFS Kernel)

on:
  workflow_call:
  workflow_dispatch:

env:
  # Device env
  DEVICE_NAME : "oneplus_8"
  DEVICE_CODENAME : "instantnoodle"

  CUSTOM_CMDS : "CLANG_TRIPLE=aarch64-linux-android-"
  # OOS 4.19.157 旧代码 + clang 19: 禁用新 clang 默认 -Werror 严格警告
  # KCFLAGS 值必须用引号包裹(含空格), 否则 make 只取第一个参数
  EXTRA_CMDS : 'KCFLAGS="-Wno-strict-prototypes -Wno-incompatible-function-pointer-types -Wno-deprecated-non-prototype -Wno-implicit-int -Wno-int-to-pointer-cast -Wno-pointer-to-int-cast -Wno-implicit-function-declaration -Wno-unused-but-set-variable"'

  # OOS 官方内核 (OnePlus OSS, 4.19.157) - 设备树在独立仓库
  KERNEL_SOURCE : "https://github.com/OnePlusOSS/android_kernel_oneplus_sm8250.git"
  KERNEL_BRANCH : "oneplus/sm8250_t_13.1_op8"

  # OOS 官方 vendor/devicetree 仓库 (含 oplus 内核模块 + kona DTS + 19821 设备树
  # + kernel/msm-4.19/techpack 的 camera/display/video 源码)
  # symlink 指向 vendor/, 需按 OnePlus 官方目录布局放置
  VENDOR_SOURCE : "https://github.com/OnePlusOSS/android_kernel_modules_and_devicetree_oneplus_sm8250.git"
  VENDOR_BRANCH : "oneplus/sm8250_t_13.1_op8"

  CLANG_SOURCE : "https://github.com/llvm/llvm-project/releases/download/llvmorg-19.1.7/LLVM-19.1.7-Linux-X64.tar.xz"
  CLANG_BRANCH : ""

  GCC_GNU : false
  GCC_64_SOURCE : "https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9.git"
  GCC_64_BRANCH : "android12L-release"
  GCC_32_SOURCE : "https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9.git"
  GCC_32_BRANCH : "android12L-release"

  DEFCONFIG_SOURCE: ""
  DEFCONFIG_NAME : "vendor/kona-perf_defconfig"
  DEFCONFIG_ORIGIN_IMAGE: ""
  DEFCONFIG_FROM_BOOT: false

  KERNELSU_SOURCE : ""
  KERNELSU_BRANCH : ""

  # 保持 latest: 每次构建都会通过 ReSukiSU setup.sh (git pull) 拉取最新 main 分支
  KERNELSU_AUTO_GET : "true"
  KERNELSU_AUTO_FORK : "resukisu"

  SUSFS_ENABLE : true
  SUSFS_FIXED : false

  AK3_SOURCE : "https://github.com/osm0sis/AnyKernel3.git"
  AK3_BRANCH : "master"

  BOOT_SOURCE : ""

  NEED_DTBO : false
  NEED_SAFE_DTBO : false

  ROM_TEXT : "oos13.1_a13"

  # Other env
  PYTHON_VERSION: "3"

  PACK_METHOD: "Anykernel3"
  PACK_KMODULES: "false"

  KERNELSU_METHOD: "shell"

  PATCHES_SOURCE: ""
  PATCHES_BRANCH: ""

  SUSFS_FOLDER_FIXED: ""
  SUSFS_PATCH_FIXED: ""

  # DroidSpaces 配置已通过 defconfig_oos.patch 注入, 无需 build-process 再 merge droidspaces.config
  DROIDSPACES_ENABLE: "false"
  NOMOUNT_ENABLE: "false"

  HOOK_METHOD: "syscall"

  KERNEL_PATCH: "false"

  KPM_ENABLE: "false"
  KPM_INLINE: "false"
  KPM_KPATCH_NEXT: "false"
  KPM_PATCH_SOURCE: ""

  BASEBAND_GUARD_ENABLE: "true"
  BASEBAND_GUARD_BOOT: "false"

  # OOS 设备树直接编译 (非overlay), 通过 OnePlus 官方 Makefile 生成
  GENERATE_DTB: "false"
  GENERATE_CHIP: "qcom"

  BUILD_DEBUGGER: "false"
  SKIP_PATCH: "false"

  # OOS defconfig 已内嵌 oplus 配置, 无需额外 merge oplus.config
  BUILD_OTHER_CONFIG: "false"
  MERGE_CONFIG_FILES: ""

  FREE_MORE_SPACE: "false"

jobs:
  build-kernel:
    name: Oneplus 8 (OxygenOS 13.1 A13) (ReSukiSU+SUSFS Kernel)
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"

    steps:
      - name: ⏰ Checkout repository
        uses: actions/checkout@v6

      - name: ⌛️ Compilation environment preparation
        uses: ./.github/workflows/build-env

      - name: ⚙️ Get neccssary tools
        uses: ./.github/workflows/build-ready

      - name: 🏗️ Setup OOS vendor/devicetree layout
        run: |
          # OnePlus OOS 内核的 vendor symlink 指向 ../../../../../../vendor
          # (从 arch/arm64/boot/dts 上溯 6 层), 需把 devicetree+oplus 源码放到对应位置
          # 布局: <ROOT>/kernel_src/arch/arm64/boot/dts -> 6层 -> <ROOT>/vendor
          # 本 workflow 中 device_kernel 位于 $GITHUB_WORKSPACE/kernel/device_kernel
          # 其 6 层上级为 $GITHUB_WORKSPACE, 故 vendor 放在 $GITHUB_WORKSPACE/vendor
          cd $GITHUB_WORKSPACE
          if [ ! -d vendor ]; then
            echo "[-] vendor/ not found, devicetree missing."
            false
          fi
          # 验证关键 symlink 可解析 (devicetree + oplus kernel)
          cd kernel/device_kernel
          for check in arch/arm64/boot/dts/vendor drivers/soc/oplus/system drivers/power/oplus fs/erofs; do
            if [ -e "$check" ]; then
              echo "[+] $check -> $(readlink -f $check)"
            else
              echo "[-] $check cannot resolve, OOS build will fail."
              false
            fi
          done
          echo "[+] OOS vendor/devicetree layout ready."

      - name: 🩹 Supply missing kona-camera-sensor-rb5.dtsi for 19066
        run: |
          # arch/arm64/boot/dts/vendor/19066/kona-v2.1-iot-rb5.dtsi includes
          # "camera/kona-camera-sensor-rb5.dtsi", which OnePlus's published
          # source has at vendor/qcom/proprietary/camera-devicetree/ but not
          # inside the 19066/camera/ folder itself. Supply it from the
          # existing published copy.
          SRC="$GITHUB_WORKSPACE/vendor/qcom/proprietary/camera-devicetree/kona-camera-sensor-rb5.dtsi"
          DEST_DIR="$GITHUB_WORKSPACE/vendor/qcom/proprietary/devicetree-4.19/19066/camera"
          if [ -f "$SRC" ]; then
            mkdir -p "$DEST_DIR"
            cp "$SRC" "$DEST_DIR/kona-camera-sensor-rb5.dtsi"
            echo "[+] Copied kona-camera-sensor-rb5.dtsi into 19066/camera/."
          else
            echo "[-] $SRC not found — check if OnePlus moved/renamed this file."
            false
          fi

      - name: 🩹 Fix broken kernel/msm-4.19 include path in charger driver
        run: |
          # oplus_battery_msm8250.{c,h}, oplus_battery_msm7250_R.{c,h}, and
          # oplus_battery_msm7250_Q.{c,h} all use
          # #include "../../../../kernel/msm-4.19/drivers/power/supply/qcom/<file>.h"
          # — 4 "../" plus a folder name (kernel/msm-4.19) that doesn't exist
          # in this layout (the kernel is at kernel/device_kernel). The real
          # target files (storm-watch.h, battery.h, smb5-reg.h, schgm-flash.h,
          # step-chg-jeita.h) already exist, untouched, at their correct real
          # location in the kernel source — the include path itself is just
          # wrong. Fix the prefix in place (one more "../" + correct folder
          # name) rather than copying files around, so every affected header
          # resolves to its real, original, already-correct file.
          DIR="$GITHUB_WORKSPACE/vendor/oplus/kernel/charger/charger_ic"
          if [ -d "$DIR" ]; then
            grep -rl 'kernel/msm-4\.19/drivers/power/supply/qcom' "$DIR" | while read -r f; do
              sed -i 's#\.\./\.\./\.\./\.\./kernel/msm-4\.19/drivers/power/supply/qcom/#../../../../../kernel/device_kernel/drivers/power/supply/qcom/#g' "$f"
              echo "[+] Fixed include paths in $f"
            done
            if grep -rq 'kernel/msm-4\.19' "$DIR"; then
              echo "[-] Some kernel/msm-4.19 references remain unfixed — check manually."
              false
            fi
          else
            echo "[-] $DIR not found — vendor layout step may not have completed."
            false
          fi

      - name: 🩹 Fix disabled oplus_router_boost build (net/Makefile)
        run: |
          # net/Makefile gates oplus_router_boost/ behind $(OPLUS_FEATURE_WIFI_ROUTERBOOST),
          # a plain Makefile variable that's never set anywhere, unlike its two
          # sibling features (OPLUS_FEATURE_DHCP=1, OPLUS_FEATURE_WIFI_CAP_CENTER=1)
          # which set the variable right above their own identical ifneq check.
          # Callers in net/ipv4/tcp_ipv4.c and net/ipv4/udp.c call
          # oplus_router_boost_handler unconditionally, so the module never
          # gets built and the symbol is undefined at link time. Add the
          # missing assignment, matching the existing pattern exactly.
          F="$GITHUB_WORKSPACE/kernel/device_kernel/net/Makefile"
          if grep -q '^ifneq (\$(OPLUS_FEATURE_WIFI_ROUTERBOOST),)$' "$F"; then
            sed -i 's/^ifneq (\$(OPLUS_FEATURE_WIFI_ROUTERBOOST),)$/OPLUS_FEATURE_WIFI_ROUTERBOOST=1\nifneq ($(OPLUS_FEATURE_WIFI_ROUTERBOOST),)/' "$F"
            echo "[+] Added OPLUS_FEATURE_WIFI_ROUTERBOOST=1 to net/Makefile."
          else
            echo "[-] Expected ifneq line not found in net/Makefile — check if OnePlus changed it."
            false
          fi

      - name: 🩹 Fix disabled oplus/ display build (techpack/display/Makefile)
        run: |
          # techpack/display/Makefile has "#obj-$(CONFIG_DRM_OPLUS) += oplus/"
          # commented out, and CONFIG_DRM_OPLUS is never set in the defconfig.
          # Uncomment and make unconditional (obj-y), since callers
          # (dsi_panel.c, msm_drv.c, sde_encoder.c, etc.) call oplus_adfr_*
          # functions unconditionally with no matching guard.
          F="$GITHUB_WORKSPACE/kernel/device_kernel/techpack/display/Makefile"
          if grep -q '^#obj-\$(CONFIG_DRM_OPLUS) += oplus/$' "$F"; then
            sed -i 's/^#obj-\$(CONFIG_DRM_OPLUS) += oplus\/$/obj-y += oplus\//' "$F"
            echo "[+] Enabled oplus/ build in techpack/display/Makefile."
          else
            echo "[-] Expected commented obj line not found — check if OnePlus changed it."
            false
          fi

      - name: 🩹 Restore oplus/Makefile (needed for the fix above to actually build)
        run: |
          # obj-y += oplus/ tells Kbuild to recurse into oplus/ and use its
          # own Makefile — but OnePlus renamed the real one to Makefile_back,
          # leaving no oplus/Makefile at all, so the previous fix alone fails
          # with "No rule to make target oplus/Makefile". Restore it from
          # Makefile_back, and make its own obj list unconditional too, since
          # it's gated behind CONFIG_DRM_OPLUS (unset) and oplus_adfr.o is
          # additionally gated behind OPLUS_FEATURE_ADFR_KERNEL (also unset),
          # same unwired-config pattern as the previous two fixes.
          F="$GITHUB_WORKSPACE/kernel/device_kernel/techpack/display/oplus/Makefile"
          BACKUP="$GITHUB_WORKSPACE/kernel/device_kernel/techpack/display/oplus/Makefile_back"
          if [ -f "$BACKUP" ]; then
            cp "$BACKUP" "$F"
            sed -i 's/obj-\$(CONFIG_DRM_OPLUS)/obj-y/g' "$F"
            sed -i 's/OPLUS_FEATURE_ADFR_KERNEL),yes)/1),1)/' "$F"
            echo "[+] Restored oplus/Makefile from Makefile_back, made unconditional."
          else
            echo "[-] $BACKUP not found — check if OnePlus renamed/removed it differently."
            false
          fi

      - name: 📌 Kernel version suffix (OOS)
        run: |
          cd $GITHUB_WORKSPACE/kernel/device_kernel
          # ReSukiSU setup.sh 已注册 drivers/kernelsu (Makefile+Kconfig)
          # 这里仅设置自定义版本后缀
          echo "-g$(git rev-parse --short HEAD)-oos-hotsteel" > .scmversion
          echo "[+] .scmversion written."

      - name: 🪝 Patch Kernel of SUSFS + ReSukiSU (OOS)
        run: |
          cd $GITHUB_WORKSPACE/kernel/device_kernel/
          cp /tmp/Patches/Patch/susfs_resukisu_oos_4.19.patch ./
          patch -p1 < susfs_resukisu_oos_4.19.patch || true
          rm -f susfs_resukisu_oos_4.19.patch
          echo "[+] OOS SUSFS + ReSukiSU patched."

      - name: 💿 Patch Kernel of Droidspaces (OOS)
        run: |
          cd $GITHUB_WORKSPACE/kernel/device_kernel/
          cp /tmp/Patches/Droidspaces/oos_droidspaces.patch ./
          patch -p1 < oos_droidspaces.patch || true
          rm -f oos_droidspaces.patch
          echo "[+] Patched DroidSpaces for OOS successfully."

      - name: 🔒 Patch Kernel of Baseband Guard (OOS, non-GKI)
        run: |
          cd $GITHUB_WORKSPACE/kernel/device_kernel/
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash
          echo "[+] Patched Baseband Guard for OOS successfully."

      - name: 📝 Apply OOS defconfig fragment
        run: |
          cd $GITHUB_WORKSPACE/kernel/device_kernel/
          cp /tmp/Patches/Patch/defconfig_oos.patch ./
          patch -p1 < defconfig_oos.patch || true
          rm -f defconfig_oos.patch
          echo "[+] OOS defconfig applied."

      - name: 🚀 Build Process
        uses: ./.github/workflows/build-process

      - name: 🖼️ Generate DTB (OOS concat kona dtb variants)
        run: |
          cd $GITHUB_WORKSPACE/kernel/device_kernel
          DTB_DIR="out/arch/arm64/boot/dts/vendor/19821"
          if [ -f "$DTB_DIR/kona-mtp.dtb" ]; then
            cat "$DTB_DIR/kona-mtp.dtb" > out/arch/arm64/boot/dtb.img
            echo "[+] Generated dtb.img from kona-mtp.dtb"
          else
            # 回退: 拼接 vendor 下所有 dtb (与 OnePlus 官方 AndroidKernel.mk 一致)
            cat out/arch/arm64/boot/dts/vendor/*/*.dtb > out/arch/arm64/boot/dtb.img
            echo "[+] Generated dtb.img (concat all vendor dtbs)"
          fi

      - name: 🔩 Pack Process
        uses: ./.github/workflows/pack-process

      - name: ✏️ Upload Artifacts
        uses: ./.github/workflows/upload-files
